import { createMuiTheme } from '@material-ui/core/styles'

export default createMuiTheme({
  palette: {
    primary: {
      main: '#0256ee',
    },
    secondary: {
      main: '#ee0255',
    },
    background: {
      default: '#FFFFFF',
      paper: '#FFFFFF',
    },
  },
})
